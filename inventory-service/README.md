# inventory-service

To be implemented. Consumes `payment.confirmed`; publishes `inventory.reserved` / `inventory.failed`; publishes `payment.refund.requested` on failure after payment (compensation).
